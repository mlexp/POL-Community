<x-public-layout title="横断検索"><h1 class="mb-5 text-2xl font-bold">日記・コミュニティ・更新情報を検索</h1><form class="mb-6 flex gap-3"><input aria-label="検索語" name="q" maxlength="100" value="{{ $term }}" class="w-full rounded border p-2"><button class="rounded border px-4">検索</button></form>
@if($results)<p class="mb-4">{{ $results->total() }}件</p><ul class="space-y-4">@foreach($results as $item)<li><span class="text-xs">{{ ['diary'=>'日記','community'=>'コミュニティ','announcement'=>'更新情報'][$item->kind] }}</span><h2><a class="underline" href="{{ route(['diary'=>'diaries.show','community'=>'community.show','announcement'=>'announcements.show'][$item->kind],$item->id) }}">{{ $item->title }}</a></h2><p>{{ $item->summary }}</p></li>@endforeach</ul><div class="my-5">{{ $results->links() }}</div>@endif</x-public-layout>
