<x-public-layout title="検索"><h1 class="mb-5 text-2xl font-bold">検索</h1><form class="mb-6 space-y-3"><div class="flex gap-3"><input aria-label="検索語" name="q" maxlength="100" value="{{ $term }}" class="min-w-0 flex-1 rounded border p-2"><button class="whitespace-nowrap rounded border px-5">検索</button></div><fieldset class="flex flex-wrap gap-4"><legend class="sr-only">検索対象</legend>@foreach(['diary'=>'日記','community'=>'コミュニティ','announcement'=>'更新情報'] as $value=>$label)<label><input type="checkbox" name="types[]" value="{{ $value }}" @checked(in_array($value,$types,true))> {{ $label }}</label>@endforeach</fieldset></form>
@if($results)<p class="mb-4">{{ $results->total() }}件</p><ul class="space-y-4">@foreach($results as $item)<li><span class="text-xs">{{ ['diary'=>'日記','community'=>'コミュニティ','announcement'=>'更新情報'][$item->kind] }}</span><h2><a class="underline" href="{{ route(['diary'=>'diaries.show','community'=>'community.show','announcement'=>'announcements.show'][$item->kind],$item->id) }}">{{ $item->title }}</a>@if($item->author && ($item->kind !== 'community' || $item->author_user_id)) - {{ $item->author }}@endif</h2><p>{{ $item->summary }}</p></li>@endforeach</ul><div class="my-5">{{ $results->links() }}</div>@endif</x-public-layout>
