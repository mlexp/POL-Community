<x-public-layout :title="$announcement->title"><article><h1 class="my-5 text-3xl font-bold">{{ $announcement->title }}</h1><p class="whitespace-pre-line">{{ $announcement->summary }}</p>@if($announcement->url && app(\App\Support\MediaUrl::class)->https($announcement->url))<a class="my-5 block underline" href="{{ $announcement->url }}" rel="noopener noreferrer">詳細を見る</a>@endif</article></x-public-layout>
