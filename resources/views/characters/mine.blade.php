<x-layouts::app title="マイキャラクター"><div class="p-6"><div class="mb-6 flex items-center justify-between"><h1 class="text-2xl font-semibold">マイキャラクター</h1><a class="rounded bg-zinc-900 px-4 py-2 text-white" href="{{ route('characters.create') }}">新規登録</a></div>
@if(session('status'))<div class="mb-4 rounded bg-green-50 p-3 text-green-900">{{ session('status') }}</div>@endif
<div class="grid gap-4 md:grid-cols-2">@forelse($characters as $character)<article class="flex gap-4 rounded border p-4"><img class="h-20 w-20 rounded object-cover" src="{{ app(\App\Support\AvatarUrl::class)->character($character) }}" alt=""><div><h2 class="text-lg font-medium"><a class="underline" href="{{ route('characters.show',$character) }}">{{ $character->name }}</a></h2>@if($character->is_primary)<span class="text-xs">メイン</span>@endif<p class="text-sm">{{ $character->short_message }}</p></div></article>@empty<p>登録されたキャラクターはありません。</p>@endforelse</div></div></x-layouts::app>
