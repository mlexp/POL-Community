<x-layouts::app title="キャラクター"><div class="p-6"><div class="mb-6 flex items-center justify-between"><h1 class="text-2xl font-semibold">キャラクター</h1><a class="rounded bg-zinc-900 px-4 py-2 text-white" href="{{ route('characters.create') }}">新規登録</a></div>
@if(session('status'))<div class="mb-4 rounded bg-green-50 p-3 text-green-900">{{ session('status') }}</div>@endif
<div class="grid gap-4 md:grid-cols-2">@forelse($characters as $character)<article class="rounded border p-4"><div class="flex justify-between"><h2 class="text-lg font-medium"><a class="underline" href="{{ route('characters.show',$character) }}">{{ $character->name }}</a></h2>@if($character->is_primary)<span class="rounded bg-blue-100 px-2 py-1 text-xs text-blue-900">メイン</span>@endif</div><p class="mt-2 line-clamp-3 text-sm text-zinc-600">{{ $character->profile_text }}</p><div class="mt-3 text-xs">公開範囲: {{ $character->visibility }}</div></article>@empty<p>登録されたキャラクターはありません。</p>@endforelse</div></div></x-layouts::app>
