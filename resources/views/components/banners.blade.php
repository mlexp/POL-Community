@props(['banners', 'align' => 'left'])
<div @class(['my-5 space-y-5', 'flex flex-col items-end' => $align === 'right', 'flex flex-col items-center' => $align === 'center'])>@foreach($banners as $banner)@if(app(\App\Support\MediaUrl::class)->https($banner->destination_url))<a class="block" href="{{ $banner->destination_url }}" target="_blank" rel="noopener noreferrer{{ $banner->rel_sponsored ? ' sponsored' : '' }}"><img loading="lazy" @class(['h-auto rounded', 'max-w-[min(360px,100%)]' => $align === 'right', 'max-w-full' => $align !== 'right']) src="{{ route('images.show',$banner->image_attachment_id) }}" alt="{{ $banner->alt_text }}"></a>@endif @endforeach</div>
