<x-layouts::app title="コミュニティカテゴリ"><div class="p-6">@include('admin._nav')<h1 class="mb-5 text-2xl font-bold">コミュニティカテゴリ</h1><x-content-errors />
@foreach($categories->prepend(null) as $category)<form class="my-5 space-y-3 rounded border p-4" method="POST" action="{{ $category ? route('admin.community-categories.update',$category->id) : route('admin.community-categories.store') }}">@csrf @if($category)@method('PUT')@endif<h2>{{ $category ? $category->name : 'カテゴリ追加' }}</h2><label class="block">名前<input name="name" class="block w-full rounded border p-2" value="{{ $category?->name }}" required></label><label class="block">スラッグ<input name="slug" class="block w-full rounded border p-2" value="{{ $category?->slug }}" required></label><label class="block">説明<input name="description" class="block w-full rounded border p-2" value="{{ $category?->description }}"></label><label>表示順<input type="number" class="rounded border p-2" name="sort_order" min="0" max="65535" value="{{ $category?->sort_order ?? 0 }}" required></label><label><input type="hidden" name="is_active" value="0"><input type="checkbox" name="is_active" value="1" @checked($category?->is_active ?? true)> 新規選択を許可</label><button class="rounded border px-4 py-2">保存</button></form>@endforeach</div></x-layouts::app>
