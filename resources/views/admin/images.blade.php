<x-layouts::app title="サイト画像"><div class="p-6">@include('admin._nav')<h1 class="mb-5 text-2xl font-bold">ヘッダー・バナー画像</h1><x-content-errors /><form class="space-y-4 rounded border p-4" method="POST" enctype="multipart/form-data" action="{{ route('admin.images.store') }}">@csrf<label class="block">画像<input class="block" type="file" name="image" accept="image/jpeg,image/png,image/webp" required></label><label>用途<select name="slot" class="rounded border p-2"><option value="">バナー用</option><option value="header">ヘッダーに設定</option></select></label><label class="block">代替テキスト<input class="block w-full rounded border p-2" name="alt_text"></label><button class="rounded border px-4 py-2">アップロード</button></form><p class="my-4">バナー用の画像は、バナー管理画面の画像一覧から選択できます。</p><div class="grid gap-4 md:grid-cols-3">@foreach($images as $image)<article class="rounded border p-3"><img loading="lazy" class="max-h-48" src="{{ route('images.show',[$image->id,'thumbnail']) }}" alt="{{ $image->original_name }}"><p>{{ $image->original_name }}</p><p class="text-xs">{{ $image->created_at }}</p></article>@endforeach</div>{{ $images->links() }}</div></x-layouts::app>
